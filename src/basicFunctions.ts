/**
 * An functions that returns the addition of two numbers
 * @param num1 first number
 * @param num2 second number
 * @returns the addition of the two numbers
 */
export function add(num1: number, num2: number): number {
  return num1 + num2;
}

console.log(add(1,2))